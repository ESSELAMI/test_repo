import 'package:flutter/material.dart';

class CleanxProductsScreen extends StatelessWidget {
  const CleanxProductsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
